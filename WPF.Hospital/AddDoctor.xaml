<Window x:Class="WPF.Hospital.AddDoctor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Doctor"
        Height="250" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <Label Grid.Row="0" Grid.ColumnSpan="2"
               Content="Add New Doctor"
               FontWeight="Bold" FontSize="16"
               HorizontalAlignment="Center"/>

        <!-- First Name -->
        <Label Grid.Row="1" Content="First Name:"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtFirstName"/>

        <!-- Last Name -->
        <Label Grid.Row="2" Content="Last Name:"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="txtLastName"/>

        <!-- Add Button -->
        <Button Grid.Row="2"
                Content="Add Doctor"
                x:Name="btnAddDoctor"
                Click="btnAddDoctor_Click"
                HorizontalAlignment="Left"
                Width="120" Grid.Column="1" Margin="10,73,0,-73"/>
    </Grid>
</Window>
