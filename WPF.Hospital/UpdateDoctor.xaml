<Window x:Class="WPF.Hospital.UpdateDoctor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Doctor"
        Height="400" Width="700"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Label Grid.Row="0"
               Content="Update Doctor"
               FontWeight="Bold"
               FontSize="16"/>

        <!-- Doctors DataGrid -->
        <DataGrid Grid.Row="1"
                  x:Name="dgDoctors"
                  AutoGenerateColumns="False"
                  IsReadOnly="False"
                  SelectionMode="Single"
                  Margin="0,10,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="50"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="150"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Update Button -->
        <Button Grid.Row="2"
                x:Name="btnUpdateDoctor"
                Content="Update Selected Doctor"
                HorizontalAlignment="Center"
                Width="200"
                Click="btnUpdateDoctor_Click"/>
    </Grid>
</Window>