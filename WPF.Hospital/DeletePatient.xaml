<Window x:Class="WPF.Hospital.DeletePatient"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Hospital"
        mc:Ignorable="d"
        Title="DeletePatient" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
               Text="Delete Patient" FontSize="24" FontWeight="Bold" 
               HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Patient ID / Name Input -->
        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" 
               Text="Patient ID:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="tbPatientId" Width="200" Margin="5"/>

        <!-- Patient Details DataGrid -->
        <DataGrid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Name="dgPatientDetails" 
              AutoGenerateColumns="False" Height="250" Margin="0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="*"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Age" Binding="{Binding Age}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" 
                HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Name="btnDelete" Content="Delete" Width="100" Height="35" Background="Red" Foreground="White" Click="btnDelete_Click"/>
        </StackPanel>
    </Grid>
</Window>
